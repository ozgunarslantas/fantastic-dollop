import { createAction } from "redux-act"

export const boxMouseEnter = createAction("BOX_MOUSE_ENTER")
export const boxMouseLeave = createAction("BOX_MOUSE_LEAVE")

export const login = createAction("LOGIN")